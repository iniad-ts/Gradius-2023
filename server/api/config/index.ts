export type Methods = {
  get: {
    resBody: {
      playerSpeed: number;
      playersize: { h: number; w: number };
      makeEnemyFrequency: number;
      enemySpeed: number;
      enemySize: { h: number; w: number };
      // screenNumber: number;
    };
  };
};
